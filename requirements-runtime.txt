# Linux 컨테이너 배포용 의존성 진입점이다.
#
# Dockerfile이 공식 CPU wheel의 torch/torchvision을 먼저 고정 설치한다.
# 이 파일은 API 공통 패키지와 모델 실행 라이브러리만 설치하며,
# macOS wheel이나 CUDA build로 PyTorch를 교체하지 않는다.
-r requirements-common.txt
ultralytics==8.4.106
