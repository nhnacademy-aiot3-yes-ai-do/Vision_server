# Linux deployment entry point.
#
# The team-approved CPU or CUDA PyTorch BASE_IMAGE must already provide a
# mutually compatible torch/torchvision pair. Installing this file must not
# replace that platform runtime with a macOS wheel or an unapproved CUDA build.
-r requirements-common.txt
